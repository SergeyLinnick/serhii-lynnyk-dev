{
	"versionGroups": [
		{
			"label": "Local workspace packages use workspace:*",
			"dependencies": ["@workspace/**"],
			"dependencyTypes": ["dev", "prod", "peer"],
			"pinVersion": "workspace:*"
		}
	]
}
