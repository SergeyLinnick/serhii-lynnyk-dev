## Workflow Rules

- Plan → present → approve → execute → verify → document
- Re-plan on scope change
- Stop and escalate on: tool failures >2 retries, scope growth, missing info, unresolvable uncertainty
