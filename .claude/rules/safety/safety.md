## Safety Rules

- No secrets/PII in code or commits
- No git commit/push without user approval
- No --force (use --force-with-lease if needed)
- Request approval for destructive actions (deleting files, dropping tables, etc.)
- Review git diff before staging
